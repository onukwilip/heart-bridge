import { TProject } from "@/utils/types";
import React, { FC } from "react";

const ProjectDetail: FC<{ project: TProject }> = ({ project }) => {
  return <div>ProjectDetail</div>;
};

export default ProjectDetail;
